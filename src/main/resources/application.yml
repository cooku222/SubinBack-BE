server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fintechdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: fintech          
    password: Fintech123!      
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create        
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

  # H2 콘솔은 이제 안 쓸 거면 꺼두기
  h2:
    console:
      enabled: false


jwt:
  secret: "very-very-secret-key-for-fintech-auth-1234567890123456"
  expiration-ms: 3600000 # 1 hour
